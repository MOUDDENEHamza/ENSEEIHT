#ifndef _DISPLAY_SHELL_H_
#define _DISPLAY_SHELL_H_

/** Clear the screen. */
void clear_screen(void);

/** Display the init bar. */
void init_bar(void);

/** Print the prompt. */
void print_prompt(void);

/** Display the end bar. */
void end_bar(void);

#endif
